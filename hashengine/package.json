{
  "name": "hashengine-napi",
  "version": "0.1.0",
  "description": "Node.js bindings for native hash engine",
  "main": "index.node",
  "scripts": {
    "build": "node ../scripts/hash-build.mjs",
    "build:debug": "cargo build && node ../scripts/hash-build.mjs"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.16.0"
  }
}
